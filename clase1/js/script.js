let a = 5;
let b = 0;

function dividir(a,b) {
    let resultado =""
    // if (b!=0){
    // resultado = a/b;
    // }
    // else {
    // resultado="Error, no existe la división entre 0"
    // }

    b !=0 ? resultado =a/b : resultado = "Error, no existe la división entre 0" 
    return resultado;
}

const resultado = dividir (a,b)
console.log(resultado)


const arreglo = [1,2,3,4,5]

// function invertir (arreglo) = {
//     let arregloInvertido =[];
//     for (i=arreglo.length ; i=0; i--) {

//     }

// }